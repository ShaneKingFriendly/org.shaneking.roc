## sk
sk:
  roc:
    zero:
      cache:
        transactional:
          enabled: true
      hello:
        cache:
          enabled: true
    persistence:
      dao:
        cache:
          enabled: true
      entity:
        cache:
          enabled: true
      hello:
        entity:
          enabled: true

## spring
spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    password:
    ## absolute
    #url: jdbc:sqlite:D:/test/sktest.roc.persistence.sqllite
    ## compiled for test(../org.shaneking.roc.persistence/target/test-classes/sktest.roc.persistence.sqllite)
    #url: jdbc:sqlite::resource:sktest.roc.persistence.sqllite
    ## relative
    url: jdbc:sqlite:src/test/resources/sktest.roc.persistence.sqllite
    username:

