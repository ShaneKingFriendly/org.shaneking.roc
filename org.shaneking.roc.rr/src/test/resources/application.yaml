## sk
sk:
  roc:
    cache:
      string:
        enabled: true
    persistence:
      entity:
        cache:
          enabled: true
    rr:
      limiting:
        enabled: true
      audit:
        enabled: true
      access:
        enabled: true
      cache:
        enabled: true
      crypto:
        enabled: true

## spring
spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    password:
    ## absolute
    #url: jdbc:sqlite:D:/test/sktest.roc.rr.sqllite
    ## compiled for test(../org.shaneking.roc.persistence/target/test-classes/sktest.roc.rr.sqllite)
    #url: jdbc:sqlite::resource:sktest.roc.rr.sqllite
    ## relative
    url: jdbc:sqlite:src/test/resources/sktest.roc.rr.sqllite
    username:

