## sk
sk:
  roc:
    cache:
      string:
        enabled: true
    persistence:
      entity:
        cache:
          enabled: true
    rr:
      limiting:
        enabled: true
      audit:
        enabled: true
      access:
        enabled: true
      cache:
        enabled: true
      crypto:
        enabled: true

## spring
spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    password:
    ## absolute
    #url: jdbc:sqlite:D:/test/sktest.sqllite
    ## compiled for test(../org.shaneking.roc.persistence/target/test-classes/sktest.sqllite)
    #url: jdbc:sqlite::resource:sktest.sqllite
    ## relative
    url: jdbc:sqlite:src/test/resources/sktest.sqllite
    username:

