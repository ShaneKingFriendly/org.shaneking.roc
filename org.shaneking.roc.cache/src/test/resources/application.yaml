sk:
  roc:
    cache:
      transactional:
        enabled: true

## spring
spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    password:
    ## absolute
    #url: jdbc:sqlite:D:/test/sktest.roc.cache.sqllite
    ## compiled for test(../org.shaneking.roc.cache/target/test-classes/sktest.roc.cache.sqllite)
    #url: jdbc:sqlite::resource:sktest.roc.cache.sqllite
    ## relative
    url: jdbc:sqlite:src/test/resources/sktest.roc.cache.sqllite
    username:
